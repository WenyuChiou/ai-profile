# v0.8.1 Collaboration Pulse — visual QA record

Date: 2026-08-23. Scope: summary card only (ADR-032). Renderer output was
screenshotted with Playwright/Chromium from the byte-exact SVGs; screenshots
live in a task-temporary directory and are deliberately not committed.

## Synthetic sparse fixture (test snapshot `summary_populated_*`)

- Light and dark, 830px native and 664px README-scale, each at 1x and 2x
  device pixel ratio: all clear, no clipping, no horizontal overflow.
- The five populated days render as distinct pulses (bins 2-4 and 8+
  heights verified visually); the 79 no-activity dates render as 2px
  baseline ticks; the 12×7 group rhythm and wider group gaps read at
  README scale; May/Jun/Jul month labels present and aligned; section
  label, one-line legend, and unpublished/zero states unchanged
  elsewhere on the card.

## Real maintainer aggregate (captured local state)

- Rendered from the maintainer's local `AIPROFILE_HOME` aggregate as
  captured at inspection time (1243 AI-attributed of 1784 scanned,
  2 providers, snapshot label 2026-08-24) via `aiprofile render` into a
  temporary directory — no repository was modified. This was a visual
  pass over real (non-synthetic) data only; it is NOT a verification of
  the then-current published `profile.json` bytes, which had moved past
  this local capture.
- Light and dark, 830px and 664px, 1x and 2x: all clear, no clipping.
- Dense-window behavior verified on real data: varied 12/24/36/48px pulse
  heights; partial accent fills visibly proportional; zero-attributed-AI
  days appear as pure neutral pulses clearly distinct from both accent
  fills and no-activity ticks; Jul/Aug month boundaries labeled; the
  pulse reads as one signature rather than a second heatmap.

## Verdict

Pass. Sparse, dense, zero, and unpublished states honest and legible in
both themes at native and README scale, 1x and 2x.
