# v0.4.10 Editorial Signal visual QA

Date: 2026-08-04  
Candidate: `codex/editorial-signal-skin` / `0.4.10`  
Fixture: `scripts/render_staging_dashboard.py` synthetic two-provider fixture

## Browser evidence

The self-contained dashboard was rendered from the current source tree to
`C:\Temp\aiprofile-editorial-dashboard.html` and opened with Playwright in
Chromium. The browser run used reduced-motion emulation and captured light and
dark screenshots at 320, 390, 768, and 1440 CSS pixels.

| Check | Result | Evidence |
|---|---|---|
| Light/dark responsive widths | PASS | `scrollWidth == innerWidth` and `body.scrollWidth == innerWidth` at 320/390/768/1440; no console/page errors |
| 200% page-scale check | PASS | CDP `Emulation.setPageScaleFactor(2)` at all four widths; visual viewport widths 160/195/384/720; document scroll width stayed equal to client width |
| Keyboard focus | PASS | Tab reached theme and provider controls; each focused control exposed a 3px visible outline |
| Provider filter | PASS | Enter on the exact `Claude` filter changed `aria-pressed` to `true` |
| Reduced motion | PASS | `prefers-reduced-motion: reduce` emulation; no runtime error and the existing reduced-motion CSS contract remained present |
| Network/CSP | PASS | zero non-file requests; CSP includes `default-src 'none'`, `connect-src 'none'`, and `font-src 'none'` |
| Static summary card | PASS | 830px light/dark sample rendered as a flat card with no perspective, polygon, gradient, animation, or external font request |

The low-opacity quarter-window rails and short section rules are alignment
guides only. They do not encode a metric or state and therefore are not used as
meaningful graphical marks. Quantitative information remains in labelled text,
bar height, and share fill with existing contrast tests.

## Artifact and privacy evidence

- `python scripts/release_smoke.py --wheel dist/ai_profile_cli-0.4.10-py3-none-any.whl --expected-version 0.4.10`: PASS, including exact-wheel install, eight outputs, deterministic repeated render, network-closed dashboard, and synthetic privacy canary sweep.
- The release candidate manifest now freezes the Ubuntu-authoritative wheel
  digest `41c91d01ee761abc5a22add1c2a2fb8d3b36e309411b5db0398a7eae7824cd7a`.
  The local Windows-built wheel used for the behavior smoke is not the release
  artifact; the GitHub Ubuntu build is authoritative per `docs/RELEASING.md`.
- Public SVG/output byte sweep over 28 files in `docs/assets`, `tests/snapshots`, and `dist`: zero private canary, email, absolute-path, or SHA-1 hits. The only HTTP token is the required SVG namespace declaration.
- Sanctioned summary and heatmap regeneration both exited 0; a repeat hash comparison reported `changed_after_repeat []`.

## Limitations

This is a deterministic Chromium smoke matrix, not a substitute for GitHub's
Markdown renderer or a physical-device assistive-technology audit. The release
workflow must still run its Ubuntu/Windows/macOS exact-wheel onboarding jobs and
the maintainer Profile Pages check before public promotion.
