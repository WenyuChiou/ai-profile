# ai-profile v0.5.0 model-family release readiness

Date: 2026-08-04
Release target: `v0.5.0` Public Beta
Branch: `codex/v050-model-categories`
Reviewer posture: independent Principal Software Engineer; verification only

## Executive summary

The candidate implementation is locally production-quality for the model-family
feature: explicit ACE model values are normalized to a closed vocabulary,
aggregated non-exclusively, redacted through the existing `VizStats` boundary,
and rendered in a flat Summary Card and dashboard without changing the CLI,
provider filters, terrain semantics, or eight-output contract.

Local gates are green: 667 passed / 4 skipped, Ruff clean, README parity pass,
sanctioned snapshot regeneration byte-stable, exact-wheel smoke pass, artifact
contract pass, browser matrix pass, and zero privacy-canary hits. CI run
`30934549387` also passed the Ubuntu-authoritative build, Python 3.11–3.14,
and exact-wheel onboarding on Ubuntu, macOS, and Windows. The candidate is not
yet a released artifact: PyPI publication and maintainer Profile refresh
remain open for v0.5.0.

## Artifact identity

| Item | Candidate value |
|---|---|
| Version | `0.5.0` (`pyproject.toml` and runtime agree) |
| ACE/public schema | `0.3.0` |
| Wheel | `ai_profile_cli-0.5.0-py3-none-any.whl` |
| Local Windows wheel SHA-256 (historical candidate evidence) | `2d67bd40b47a32125c5e609873626c1c64f92d79d4ca83a58ce608db88a41181` |
| Ubuntu CI authoritative wheel SHA-256 (release pin) | `dcd407fa5a570b1a47ba3c613998f681c5c992f10f18119ab4f4be457221f245` |
| Ubuntu CI run | `30934549387` |
| Staging dashboard SHA-256 | `cace8ed2b4f61affb0661e5ba3beae9de42836cc025ce8334b76b4226609110e` |
| Source date epoch | `1785801600` |

The local Windows wheel is retained only as historical candidate evidence. The
Ubuntu CI digest is the release authority; Windows and macOS passed onboarding
against the exact retained CI bytes.

## Verification evidence

| Gate | Command/evidence | Result |
|---|---|---|
| Full tests | `python -m pytest tests -p no:cacheprovider` | 667 passed, 4 skipped |
| Ruff | `python -m ruff check src tests scripts` | All checks passed |
| README parity | `python scripts/check_readme_parity.py` | PASS |
| Snapshots/assets | sanctioned summary + heatmap scripts, repeated | zero diff on second run; 25 files |
| Packaging | `python -m build`; `python -m twine check ...`; `python scripts/check_release_artifacts.py` | PASS |
| Clean wheel | exact-wheel `scripts/release_smoke.py` | PASS |
| Browser | Chromium 320/390/768/1280/1440, light/dark, reduced motion, effective 200% | PASS |
| Privacy | 27 public/artifact files byte-sweep | zero canary/email/path/SHA hits |
| Independent code review | model-family reviewer + silent-failure scan | APPROVE; no Critical/High finding |

## Findings

### Medium — publication and live Profile gates are still open

**Description:** The authoritative Ubuntu build and exact-wheel onboarding on
Ubuntu, Windows, and macOS passed in CI run `30934549387`. The tag/PyPI
publication and maintainer Profile refresh have not yet been completed.

**Impact:** Until publication and live Profile checks complete, users cannot
install the v0.5.0 candidate from PyPI and the public example still points at
the previous release.

**Recommendation:** Keep the frozen Ubuntu digest, merge the reviewed PR,
publish the exact verified artifacts, rerun a clean PyPI install, and only then
refresh and verify the maintainer Profile and Pages outputs.

### Low — additive legacy `VizStats` compatibility

**Description:** A manually constructed legacy `VizStats` may keep `models=()`
with nonzero AI totals. The production privacy builder supplies model rows for
every AI/mixed presence and the renderer states when explicit model evidence is
not published.

**Impact:** External callers bypassing the production builder can omit model
evidence without a validation error.

**Recommendation:** Keep the additive compatibility in v0.5.0; document it as
an internal fixture compatibility rule and consider requiring model rows in a
future contract/schema bump.

## Final recommendation

**NO-GO — pending publication and live Profile verification.**
