# v0.9.0 Collaboration Mine — release readiness

Date: 2026-09-06. Status: local candidate gates passed; GitHub
CI/publication/cloud deployment pending. This is not a live-release claim.

## Candidate identity

- Version: 0.9.0 Public Beta; ACE remains 0.3.0.
- Canonical Ubuntu CI runtime: CPython 3.12.14 from official
  actions/python-versions release `3.12.14-31661455385`.
- Runtime archive SHA-256:
  `5a03168292516f6dd6dcf630f4bf5369b108abd7c699a7e1db37f1e622257fff`.
- Build 1.4.3, Hatchling 1.31.0, Twine 6.2.0; SOURCE_DATE_EPOCH 1786320000.
- Wheel: `ai_profile_cli-0.9.0-py3-none-any.whl`.
- Wheel SHA-256:
  `b37d9e0c4e8641412777c7b17265c15e4e3fcab51a51a5cbf41d8e1e3fef0128`.
- Installed-wheel synthetic staging dashboard SHA-256:
  `42f495f186eb61ec8de3c3eb699a146d882e78e8ad11f7839edc8408b7ca62d8`.
- The first candidate was superseded after independent accessibility findings;
  its receipts remain retained, not reused as current-wheel acceptance.

## Local gates and independent review

- Both sanctioned snapshot commands regenerate with no unstaged asset drift.
- Ruff clean, README parity PASS, multi-locale preset 11 checks PASS,
  `npx impeccable --json` returns `[]`.
- Installed exact-wheel smoke: all steps PASS, including eight outputs,
  deterministic repeat, faithful dry-run and zero privacy-canary hits.
- Current visual evidence includes 32 render checks and 54 new accessibility
  checks across 1440/1024/768/390/320/195px, standard and zoomed rows. Prior
  approved geometry/motion receipts cover light/dark, actual 830/664px 1x/2x
  rasters, first/mid/end frames, static print and reduced motion.
- Independent review confirms clear desktop/mobile images, closed SVG geometry,
  actor-only motion, escaped JSON and unchanged CSP/privacy boundaries. Two P2
  keyboard/table findings were fixed red-first and independently rechecked PASS.
- Full Windows Python 3.14 run before the final runbook-test correction:
  1008 passed, 30 skipped, 1 failed in 410.63s. Failure was the stale v0.7.0
  workflow SHA assertion; the corrected nine-test release-contract family passes.
  The complete corrected run then passed: **1009 passed, 30 skipped in 312.58s**
  on Windows Python 3.14. This is a fresh full-suite result, not an inferred sum.

## Four README-only roles — exact R2 wheel

Each independent role received only both README copies, the wheel and its role
objective; all fixture homes, repositories and environments were isolated.

| Role | Exact observed commits / AI / presences / Human / Unknown | Result |
|---|---|---|
| Newcomer | 5 / 3 / 4 / 1 / 1 | PASS; 8 deterministic outputs, 18 canaries with zero hits |
| Privacy | 7 / 4 / 4 / 1 / 2 | PASS; full-only dates, excluded contributes zero; 55 canaries with zero hits |
| Providers | 5 / 3 / 5 / 1 / 1 | 13/13 PASS; overlapping provider counts retained; Low README denominator friction |
| Publisher | 3 / 1 / 1 / 1 / 1 | 12/12 PASS; 24 commands exit zero, 8 deterministic files, 12 canaries with zero hits |

The root independently read all four profile.json files and exact file sets,
recomputed these totals/evidence sums, and swept 51 fixture canaries plus two
private dates across 16 privacy assets: 848 comparisons, zero hits. Published
dates were checked as positive controls. The provider role learned the evidence
record denominator from the public CLI, without an outside product hint; this
Low documentation friction is accepted with a maintainer glossary follow-up.

These are local fixtures, not GitHub/Pages tests. Original probe errors remain
recorded (calendar scaffold bounds, dry-run wording); no product failure is
hidden. No role inspected implementation/tests or created a local schedule.

## Deployment gates still required

1. All eight required GitHub CI jobs pass; merge preserves immutable workflow
   C1 `8b145e49e2805030c0e4473c1d2c821e1397389a`.
2. Publish v0.9.0 and verify exact public package digests.
3. Activate the reviewed Profile caller only after publication, run changed
   exact-eight refresh plus Pages, then unchanged with no new generated commit.
4. Verify live HTTP 200, eight assets, light/dark/mobile and enabled daily
   GitHub-hosted schedule. No local scheduler is authorized for this deployment.

## Evidence retention and cleanup

Primary marked scratch root: `C:/aiprofile-v090-preview-20260906`, owner
`aiprofile-v090-voxel-world`, expiry 2026-09-13 while the release checklist is
open. Raw role ledgers are under `dogfood-newcomer`, `dogfood-privacy`,
`dogfood-providers`, `dogfood-publisher`; latest corrected receipts use `-r2`
for the first two. Root recomputation is `.coord/dogfood-recomputation-r2.json`.
Browser evidence is `mine-codex-qa/release-a11y-results.json`.

The single marked Ubuntu build root is `/tmp/aiprofile-v090-release-20260906`
(expiry 2026-09-07); remove it after retaining required release evidence.
Windows TEMP and TMP are `C:/Users/wenyu/AppData/Local/Temp`; POSIX disposable
root is `/tmp`. Existing unmarked Windows candidates remain UNKNOWN and are
not deletion-authorized. Protect `C:/aiprofile-private` and both Git worktrees.
