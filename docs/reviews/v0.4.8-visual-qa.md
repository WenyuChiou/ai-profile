# v0.4.8 visual QA

Date: 2026-08-01

Candidate range:
`374118a88840388a0a374d5cab695b6f18a49c2c..04c27504ed02c83ccdd0ef657558d65f6798eb80`

Candidate wheel SHA-256:
`d8d307d4155f58f157ee817cdd628ef4c257287083aad66cf30e02f679fe47b6`

Dashboard SHA-256:
`c8680c2812343077775c2b5c0fddae9dce32c1517bbaa4c920e056b347fdbd4f`

## Scope and method

This report covers the pre-release renderer and dashboard candidate. It uses
the sanctioned sample SVGs and the dashboard produced by the exact retained
wheel. Browser probes used a real Chromium tab; assertions also inspect the
rendered DOM and computed layout. Synthetic populated, aggregate-only, and
zero states were covered by browser or regression fixtures. The live personal
Profile and public Pages rows are completed after the protected `main` merge,
then rechecked in the final release-readiness report.

Screenshot evidence:

- [Summary, light](evidence/v0.4.8/summary-light.png)
- [Summary, dark](evidence/v0.4.8/summary-dark.png)
- [Dashboard, All AI/system](evidence/v0.4.8/dashboard-all-system.png)
- [Dashboard, OpenAI/dark](evidence/v0.4.8/dashboard-openai-dark.png)

The four files were decoded and re-exported as genuine lossless PNGs, then
swept byte-for-byte for local-path, localhost, e-mail, fixture-root, and
account markers: zero hits. Their SHA-256 values, in the order listed above,
are:

```text

```

## Summary-card review

| Surface | Theme | Result | Evidence |
| --- | --- | --- | --- |
| Populated synthetic | Light | PASS | 830px card; no clipping; hero, secondary metrics, terrain, top-six ledger, evidence rail, privacy cue, and footnote remain visually distinct |
| Populated synthetic | Dark | PASS | Same geometry and content as light; navy surfaces, blue signal, and gold evidence cues preserve hierarchy without large saturated blocks |
| Sparse/mixed | Light and dark | PASS | Fixed height/share bins and terrain alignment remain stable with isolated activity |
| Aggregate-only | Light and dark | PASS | Neutral `Daily activity is not published for this profile` state; aggregate totals remain available without dates |
| Zero | Light and dark | PASS | Onboarding zero state remains distinct from withheld daily data |
| Long provider/top-six | Light and dark | PASS | Provider names and right-aligned numeric columns remain within the card; overflow is summarized rather than extending geometry |

Semantic visual checks passed:

- terrain height uses unique daily commits, never provider totals;
- one multi-provider commit does not create multiple terrain blocks;
- terrain color expresses AI share while labels and bin legends carry the
  meaning without relying on color alone;
- unknown and Human-Only remain separate labels and counts;
- aggregate-only activity cannot reveal terrain dates;
- display/body/number stacks use only local IBM Plex fallbacks and system
  fonts; no external font request exists.

## Dashboard viewport matrix

The following values are CSS-pixel document/client width measurements. Every
row had `scrollWidth == clientWidth` for both `documentElement` and `body`.

| Browser width | Effective width at 200% | Normal result | 200% result |
| ---: | ---: | --- | --- |
| 320 | 145 | PASS | PASS |
| 390 | 180 | PASS | PASS |
| 768 | 369 | PASS | PASS |
| 1280 | 625 | PASS | PASS |
| 1440 | 705 | PASS | PASS |

The effective widths reflect browser chrome and device-pixel rounding rather
than a simple division. The dashboard reflows to one column, keeps its
provider controls readable, and does not introduce document-level horizontal
overflow. The calendar's own bounded scroller remains intentional.

## Theme, filter, and input matrix

| Check | Result | Reproduced evidence |
| --- | --- | --- |
| System/light/dark theme cycle | PASS | Accessible label and visible value stayed synchronized: auto -> light -> dark -> auto |
| All AI filter | PASS | Pressed state and live status reported all-provider activity; unique commit semantics retained |
| Claude filter | PASS | Provider-specific values and marks updated without changing the all-record unattributed count |
| OpenAI filter | PASS | Pressed state, live status, hero, presences, and active days updated consistently |
| Tab navigation | PASS | Interactive controls remained reachable in document order with a visible focus indicator |
| Arrow keys | PASS | ArrowLeft moved a focused calendar cell back exactly one week |
| Enter | PASS | Provider/calendar controls activated through keyboard paths |
| Escape | PASS | Open calendar tooltip dismissed and its association was removed |
| Focus | PASS | Active calendar cell exposed a 3px solid outline; selected provider also had text/shape state, not color alone |
| Coarse pointer | PASS | Primary controls keep a 44px minimum; calendar cells expand to 16px with 8px gaps, preserving 24px center spacing |
| Reduced motion | PASS | The reduced-motion media rule disables transition and animation duration; SVGs contain no animation |
| Tooltip containment | PASS | X position is clamped using half tooltip width; narrow/mobile probes remained inside the viewport |

## Contrast and static-safety checks

- Automated palette assertions require normal text/background pairs to meet
  WCAG 4.5:1 and meaningful marks to meet 3:1 in both themes.
- The selected-provider label stays at normal text color; provider brand color
  is limited to icon, border, mark, and bar surfaces.
- All summary text is at least 11px. The locked type scale is
  `11/12/13/16/38`, with 400 labels, 600 metrics, and 700 hero values.
- SVG element and attribute allowlists passed; no script, external reference,
  event handler, animation, gradient, or network dependency is present.
- The dashboard CSP is network-closed and the candidate's eight outputs are
  deterministic.

Targeted evidence commands:

```text
python -m pytest tests/unit/test_recruiter_card.py tests/unit/test_dashboard_html.py tests/unit/test_render_summary.py -p no:cacheprovider
python tests/unit/test_render_summary.py
python tests/unit/test_render_summary.py
python -m ruff check src tests scripts
```

The targeted pytest run reported `93 passed in 0.28s`; Ruff reported
`All checks passed!`. The sanctioned regeneration ran twice with no byte
drift and imported `aiprofile` from this worktree on both runs. The complete suite
and cross-platform wheel onboarding evidence are recorded in
`gate-review.md`.

## Staging disposition

The manual staging workflow run `30716848131` rebuilt the authoritative wheel,
rendered the dashboard, verified both pinned digests, and uploaded only the
two-file staging tree. Deployment from the PR branch was correctly denied by
the `github-pages` environment's main-only protection rule. This is not a
renderer failure and the protection was not weakened. The same workflow must
run from merged `main`, and the public URL plus bytes must pass before the
v0.4.8 tag is published.

**Post-merge resolution (2026-08-01):** the same staging workflow ran from
merged `main` as run `30717400004`, passed, and deployed the exact pinned
dashboard bytes. The public staging URL serves the dashboard with SHA-256
`c8680c2812343077775c2b5c0fddae9dce32c1517bbaa4c920e056b347fdbd4f`, matching
the candidate digest above. The release workflow run `30717707873` also
passed. The pre-merge blocking condition in the previous paragraph is
therefore satisfied.

## Live Profile and public verification (post-merge, 2026-08-01)

The maintainer Profile refresh on public PyPI 0.4.8 merged as Profile PR #12
at `7f322beab1e532d906f943c62fc68e49ba21c02a`; Profile Pages run
`30718799030` passed. All eight published outputs are HTTP 200 and
byte-identical to the merged git blobs; the live profile dashboard's SHA-256
is `b17e996a6fb0fa8530779c59d47d5cf069154ffd8437e431d2bf7331c12ac292`. The
GitHub Profile render shows exactly one Summary card, zero remnants of the
old 3D card, and zero extra heatmaps, on both GitHub desktop and mobile
(HTTP 200).

Live browser matrix against the public dashboard and Profile assets:

| Check | Result |
| --- | --- |
| Viewports 320 / 390 / 768 / 1280 / 1440 | PASS |
| 200% zoom | PASS |
| Light / dark / system themes | PASS |
| All AI / Claude / OpenAI filters | PASS |
| Keyboard Tab / Arrow / Enter / Escape | PASS |
| Visible focus indicator | PASS |
| Reduced-motion source | PASS |
| Tooltip containment | PASS |
| No document-level horizontal overflow | PASS |

**Accepted limitation (mobile):** the Summary card is fixed-width at 830px,
so its detail text shrinks on mobile. This is explicitly accepted for
v0.4.8: the hero metric, the top compact badge, and the surrounding README
text remain readable, and the linked dashboard is responsive. No ninth
output is to be added for this.

## Findings

No unresolved visual, accessibility, privacy, or interaction finding was
reproduced in the candidate or in the live post-merge verification. Two
accepted nonblockers are recorded: the Low README wording gap (the README
does not explicitly state that Human-Only belongs to the declared evidence
bucket — a v0.4.9 documentation follow-up) and the fixed-width mobile
limitation above. GitHub Pages reports an upstream Node 20 deprecation
annotation, but build and deploy pass and no repo-controlled action fails.

## Candidate verdict

**PASS FOR PROTECTED-MAIN MERGE.** (Pre-release verdict, retained as
recorded: release remained blocked until the main-only staging deployment
and live Profile verification completed.)

## Final verdict (2026-08-01)

**PASS FOR PUBLIC RELEASE.** The main-only staging deployment, the release
run, and the live Profile verification all completed successfully with
byte-exact artifacts.
