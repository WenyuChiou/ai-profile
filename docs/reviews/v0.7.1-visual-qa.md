# ai-profile v0.7.1 visual QA

Date: 2026-08-10
Surface: live maintainer Profile at merge
`7e14cb593fde12bca00747aba71805b536e31588`
Pages run: [`31411007440`](https://github.com/WenyuChiou/WenyuChiou/actions/runs/31411007440)

## Result

The released v0.7.1 Profile dashboard, Summary Card, heatmap, and badge remain
clear in light and dark presentation. The visual is flat 2D, uses visible text
labels, and keeps unknown evidence separate rather than inferring human work.
No clipping, overlap, horizontal overflow, missing accessible labels, or
contrast failure was observed in the recorded matrix.

## Fresh live-browser evidence

| Check | Result |
|---|---|
| Responsive widths | 390px and 1280px: zero document-level horizontal overflow |
| Minimum rendered font | 13px at both tested widths |
| Provider filters | All AI `1207`; Claude `1139`; OpenAI `107` |
| Themes | Live light and dark changes passed |
| Minimum normal-text contrast | light `4.6129:1`; dark `8.1352:1` |
| Contrast failures | 0 |
| Calendar controls | 309 native day buttons; 0 missing accessible labels |

Provider rows are non-exclusive by design, so Claude plus OpenAI may exceed
the `1207` unique AI-attributed commits. The filter values agree with the live
public aggregate; they are not skill scores.

### Keyboard evidence boundary

The fresh automation driver reliably exercised the live provider and theme
controls. It did not provide a trustworthy fresh Arrow/Escape traversal, so
this record does not claim one. The live dashboard exposes native calendar
buttons with complete labels, and its source retains the Arrow and Escape
handlers. Arrow movement and Escape dismissal were previously exercised live
on this same renderer and remain pinned by automated dashboard tests. This is
a driver limitation, not a silent pass or a fresh manual-keyboard claim.

## Static visual evidence

- Light and dark Summary Cards were rasterized at 2×. Both were crisp, flat,
  and free of clipping or overlap; headings, totals, daily matrix, provider
  ledger, evidence rail, and footer remained readable.
- Light and dark heatmaps preserved clear cells, month/day labels, legends,
  and the publishable-repositories boundary.
- Light and dark badges remained compact and legible without clipped text.
- The visual family stayed deterministic and self-contained. No renderer,
  schema, aggregation, privacy, or eight-file contract changed in v0.7.1.

## Live-byte identity

All eight [live Profile assets](https://wenyuchiou.github.io/WenyuChiou/dist/dashboard.html)
returned HTTP 200 and byte-matched the files at
Profile main commit `7e14cb593fde12bca00747aba71805b536e31588`. The live and
raw dashboard SHA-256 is
`b600647950882adedb980af2ffff5571cfc1e0df2da40afc4f9c8502f8b6eb1c`.

## Final verdict

**GO — PUBLIC BETA**
