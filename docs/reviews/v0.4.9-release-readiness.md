# ai-profile v0.4.9 Flat Evidence Ledger release readiness

Date: 2026-08-04
Release: `v0.4.9` Public Beta
Reviewer posture: independent Principal Software Engineer; verification only

## Executive summary

The flat Evidence Ledger design is now the released v0.4.9 Public Beta. The
static summary is a deterministic 2D matrix: unique daily commit volume drives
track/bar size, AI share drives the fill ramp, and provider overlap never
changes geometry. The existing eight-output contract, `VizStats` boundary,
aggregation semantics, privacy model, CLI, and schema are unchanged.

The package, GitHub Release, PyPI install, maintainer Profile, and GitHub Pages
deployment were verified from the same release candidate. No unresolved
Critical or High findings remain.

## Release identity

| Item | Verified value |
|---|---|
| Tag / main commit | `v0.4.9` / `876bab3` |
| GitHub Release | [v0.4.9 Public Beta](https://github.com/WenyuChiou/ai-profile/releases/tag/v0.4.9) |
| PyPI | [ai-profile-cli 0.4.9](https://pypi.org/project/ai-profile-cli/0.4.9/) |
| Wheel SHA-256 | `f04e6c33b72072190e1cb18fbb154897c25ec7986fd316427d807c81e49fb468` |
| sdist SHA-256 | `fe4d2b4723f030d9de1880f0b38baa8ef2904fb92a28366ed730a91cf5cb2053` |
| Release workflow | `30913451763` — success |
| Main CI | `30913298790` — success |
| Profile merge / Pages | `b991504` / `30915613152` — success |

## Verification evidence

- Full suite: `629 passed, 4 skipped`.
- Ruff: `All checks passed!`.
- README parity: `PASS`.
- Summary and heatmap sanctioned regeneration completed with zero repeat-run
  byte drift.
- Release artifact contract, Twine checks, and exact-wheel smoke passed.
- PyPI clean install reported `aiprofile 0.4.9`; the release smoke covered
  init, scan, aggregate, render, determinism, CSP, and privacy canary checks.
- The maintainer Profile was regenerated from the exact PyPI v0.4.9 wheel and
  the existing eleven-repository publication scope. All eight dist files are
  HTTP 200 and match the merged Git blobs after LF normalization.
- Live dashboard browser verification passed at 320, 390, 768, and 1440px:
  no page-level horizontal overflow, provider filtering, visible focus, and
  CSP present.
- Dist privacy sweep found zero repository paths/names, organization names,
  SHAs, emails, prompts, commit subjects/bodies, or secrets. Unknown remains
  separate from human, and provider totals remain non-exclusive.
- Both repositories have clean `main` worktrees; merged feature branches and
  stale remote refs were pruned.

## Verified visual and maintainability areas

- The Summary Card is flat and editorial: no perspective, prism, gradient,
  external font, network request, or decorative 3D surface.
- Local fallback typography remains deterministic and free of paid fonts.
- Renderers still consume validated aggregate data only; they do not scan Git,
  open SQLite, infer attribution, or recalculate statistics.
- The dashboard remains the interactive surface; README embeds remain static,
  clickable, theme-aware SVGs.

## Findings

No unresolved Critical, High, Medium, or Low findings. GitHub Pages reported
an informational Node.js deprecation annotation for its hosted action runtime;
it does not affect the deployed artifact and is outside this repository's
runtime or release bundle.

## Final recommendation

**GO — PUBLIC BETA**
